# This script is for one time running of anything that I'd want in a development environment for Debian based distros like Ubuntu.

sudo apt update
sudo apt install vim tree

# Idea: Would be cool to have something in here that injects AddToBashRC.sh into my bash rc file. Then do
# source ~/.bashrc